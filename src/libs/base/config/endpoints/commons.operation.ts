//const AUTH_BASE = 'auth';

export const OPERATIONS_COMMONS = {
  PROVINCES: 'provinces',
  MUNICIPALITIES_BY_PROVINCE: 'municipality',
  ANIMAL_TYPES: 'animal-types',
  SIZES: 'sizes',
  COLORS: 'colors',
  SEXES: 'sexes',
  BREEDS: 'breeds',
  AGES: 'ages',
};

/* function operationJoin(OPERATION: string) {
  return `${AUTH_BASE}/${OPERATION}`;
}
 */
